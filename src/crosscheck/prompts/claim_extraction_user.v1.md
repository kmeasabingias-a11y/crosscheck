Extract all atomic, decontextualized claims from the chunks below. Each chunk is labeled with its `chunk_id` on
its own line, followed by the chunk text. Attach every claim to the `chunk_id` it came from, and quote evidence
verbatim from that chunk only.

{{chunks}}
