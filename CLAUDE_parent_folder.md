# CLAUDE.md — Workspace Pointer

This is the workspace root, not the project. The active project is **CrossCheck**, located in `./crosscheck/`.

**Before doing anything:** read `./crosscheck/CLAUDE.md` in full. It contains:
- **Part 1 — Working Agreement:** six non-negotiable rules for how you operate (hand-over mode, no git push, walkthrough folder, DECISIONS.md, always recommend, no authorship attribution). These override your defaults.
- **Part 2 — Project Specification (v2):** the complete build plan.

Other folders in this workspace:
- `./Crosscheck_Code_Walkthrough/` — you maintain this (see Rule 3 in the working agreement).
- `./Kestrel_Code_Walkthrough/` and `./TypeWright_Code_Walkthrough/` — read-only style references for how walkthroughs are written. Do not modify them.

All code work happens inside `./crosscheck/`. Do not create project files at this workspace root.
